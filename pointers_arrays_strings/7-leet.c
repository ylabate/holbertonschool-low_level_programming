#include "main.h"
/**
 * leet - encodes a string into 1337
 * @str: pointer to the string
 *
 * Return: pointer to the resulting string
 */

char *leet(char *str)
{
	int i;

	for (i = 0; str[i] != '\0'; i++)
		str = str;
return (str);
}
